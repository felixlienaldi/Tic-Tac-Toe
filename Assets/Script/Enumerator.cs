using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Enumerator {
    public enum Type{
        Circle,
        Cross,
        Default
    }

    public enum PlayerType {
        Player1 = 0,
        Player2
    }
}